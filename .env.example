# Copy to .env.local and fill in from the Supabase dashboard (Project Settings → API).
# Server-only: no NEXT_PUBLIC_ prefix, so these never ship to the browser.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
